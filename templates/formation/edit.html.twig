{% extends 'base.html.twig' %}

{% block title %}Editer Formation{% endblock %}

{% block body %}
<div class="container">
    <h1>Modifier cette formation</h1>
    
    {{ include('formation/_form.html.twig', {'button_label': 'Modifier'}) }}

    {{ include('formation/_delete_form.html.twig') }}

</div>
  <div class="text-center">
      <a href="{{ path('app_formation_index') }}" class="btn btn-warning btn-md mb-3">retour à la liste</a>
      </div>
{% endblock %}
