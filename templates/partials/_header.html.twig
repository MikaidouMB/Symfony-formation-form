{% block body %}
<nav class="navbar navbar-expand-lg navbar-py-5 bg-dark">
        <a class="navbar-brand text-white" href="{{ path('app_login') }}">
        Accueil
        </a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse " id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto ">
            {% if not app.user %}
                <li class="nav-item"><a class="nav-link text-white" href="{{ path('app_login') }}">Connexion</a></li>
                <li class="nav-item"><a class="nav-link text-white" href="{{ path('app_register')}}">Inscription</a></li>
            {% else %}
                <li class="nav-item"><a class="nav-link text-white" href="{{ path('app_formation_index') }}">Mes formations</a></li>
                <li class="nav-item"><a class="nav-link text-white" href="{{ path('app_logout') }}">Déconnexion</a></li>
            {% endif %}
        </ul>
    </div>
</nav>
{% endblock %}